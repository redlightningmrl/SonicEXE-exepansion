--hello. welcome to sonic.exe exepantion shader docs.

--hello. Welcome to the SONIC.EXE: THE CANCELLATION shader documentation.
--Here I introduce some shaders lua.

--default effects:

addVCREffect('camera')  --add VCR effect. camera can enter game (in-game) or camHUD (in-game)

addGrayscaleEffect('camera') -- add gratscale

add3DEffect('camera') -- add 3d effect(example:sunshine)

addPulseEffect('camera') -- add puluse effect (its verry cool)

addGlitchEffect('camera')-- add glitch effect to camera

addChromaticAbberationEffect('camera')--ChromaticAberration(its need a lot of resources)

--other:

createShaders('shadername')--add Shader from custom.flag file(not recommended)

clearShadersFromCamera('camera')--remove shader from camera
